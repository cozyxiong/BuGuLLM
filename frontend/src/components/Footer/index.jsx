/**
 * 二次开发：不再展示 AnythingLLM 官方 GitHub / Docs / Discord。
 * 侧栏底部仅保留设置入口（见 ActiveWorkspaces 竖向 Tab 下方）。
 */
export const MAX_ICONS = 0;
export const ICON_COMPONENTS = {};

export default function Footer() {
  return null;
}
